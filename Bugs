GMT Regex will match GMT+00:0 which is incorrect.
GMT Tooltip references UTC when it shouldn't.

----- Check above bugs are actually resolved. ----

BUG -  If you import data other than for the one selected after downloading data from the set instrument the import will fail.

